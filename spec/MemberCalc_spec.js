import { MemberCalc } from '../MemberCalc.js'

describe("the number of friends is", function() {
    it("the cost is", function() {
      let total = MemberCalc(10, 0);
      expect(total).toBe(10);
    });
  });
  